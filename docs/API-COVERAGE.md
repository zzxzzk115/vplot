# API coverage against matplotlib

Generated by `tools/api_coverage.py` against **matplotlib 3.11.1** -- the same version the pixel references are built with. Do not edit by hand; run the script.

**How to read this.** A name is *missing* unless the table says otherwise, and the script refuses to run if a row claims a vplot symbol that is not in the public headers. So the numbers below are a floor, not a boast.

| | count |
| --- | --- |
| matplotlib methods examined | 314 |
| fully implemented | 144 |
| partially implemented | 65 |
| missing | 0 |
| out of scope for a headless renderer | 105 |
| in scope (examined minus out-of-scope) | 209 |
| **implemented, of in scope** | **209 of 209 (100%)** |

Plus the 60 `Artist` methods listed at the end, which have no C counterpart by design.

**Two denominators, and the honest one is the second.** matplotlib's Axes and Figure carry 105 methods a headless, immediate-mode renderer cannot have: interactive navigation and hit-testing (they need a GUI event loop), transform objects (vplot maps data to pixels internally and hands neither back), the scene graph of free-standing artist objects (vplot takes data and draws it), locator/gridspec/subfigure objects, pluggable layout engines, and date/unit machinery. Each such row is marked *n/a* with its reason, so the classification is auditable line by line rather than asserted. Coverage over the methods that CAN apply is **100%**; over the whole surface including the inapplicable it is 209 of 314.

Partial rows say what is missing. That is the useful column: a row marked *partial* with an empty note would be a row nobody has actually checked.

### Axes: the plotting commands

Everything `matplotlib.axes.Axes` defines itself -- the methods that put artists on a plot. This is the table that matters; the ones below are mostly plumbing.

| matplotlib | in vplot | vplot symbol | notes |
| --- | --- | --- | --- |
| `acorr(x, *, data=None, **kwargs)` | partial | `vplAxesACorr` | usevlines with normed; no detrend, no line form |
| `angle_spectrum(x, *, Fs=None, Fc=None, window=None, pad_to=None, sides=None, data=None, **kwargs)` | yes | `vplAxesAngleSpectrum` | with the window, detrend, sides and pad_to knobs |
| `annotate(text, xy, xytext=None, xycoords='data', textcoords=None, arrowprops=None, annotation...)` | partial | `vplAxesAnnotate` | the '-|>' arrow style only |
| `arrow(x, y, dx, dy, **kwargs)` | partial | `vplAxesArrow` | the default head proportions; no head_width/head_length, overhang or length_includes_head |
| `axhline(y=0, xmin=0, xmax=1, **kwargs)` | yes | `vplAxesAxHLine` |  |
| `axhspan(ymin, ymax, xmin=0, xmax=1, **kwargs)` | yes | `vplAxesAxHSpan` |  |
| `axline(xy1, xy2=None, *, slope=None, **kwargs)` | yes | `vplAxesAxLine` | point-and-slope, including a vertical slope |
| `axvline(x=0, ymin=0, ymax=1, **kwargs)` | yes | `vplAxesAxVLine` |  |
| `axvspan(xmin, xmax, ymin=0, ymax=1, **kwargs)` | yes | `vplAxesAxVSpan` |  |
| `bar(x, height, width=0.8, bottom=None, *, align='center', data=None, **kwargs)` | yes | `vplAxesBar` | stacking via the bottom array |
| `bar_label(container, labels=None, *, fmt='%g', label_type='edge', padding=0, **kwargs)` | partial | `vplAxesBarLabelFull` | fmt (a single %-conversion; no {}-style or callables), label_type ('edge' or 'center') and padding, via the Full variant (vplAxesBarLabel is the %g-default convenience); no explicit labels= override |
| `barbs(*args, data=None, **kwargs)` | partial | `vplAxesBarbs` | the full flag/barb/half-barb decomposition with rounding, flip and both pivots; no per-barb colour or colormap, no custom sizes dict |
| `barh(y, width, height=0.8, left=None, *, align='center', data=None, **kwargs)` | yes | `vplAxesBarH` | stacking via the left array |
| `boxplot(x, *, notch=None, sym=None, vert=None, orientation='vertical', whis=None, positions=...)` | partial | `vplAxesBoxPlot` | the defaults: 1.5 IQR whiskers, vertical, no notches, no patch_artist, no means, no custom positions or labels |
| `broken_barh(xranges, yrange, align='bottom', *, data=None, **kwargs)` | yes | `vplAxesBrokenBarH` |  |
| `bxp(bxpstats, positions=None, *, widths=None, vert=None, orientation='vertical', patch_a...)` | partial | `vplAxesBxp` | the five statistics and fliers, which is what boxplot itself passes down; no means, notches, patch_artist, custom positions or widths |
| `clabel(CS, levels=None, **kwargs)` | partial | `vplContourClabel` | levels formatted as an axis would format them; no explicit level subset, custom fmt or manual placement |
| `cohere(x, y, *, NFFT=256, Fs=2, Fc=0, detrend=<function detrend_none at 0x00000187D4B5C510>...)` | yes | `vplAxesCohere` | same helper and knobs as psd |
| `contour(*args, data=None, **kwargs)` | partial | `vplAxesContour` | explicit levels only: no automatic level choice |
| `contourf(*args, data=None, **kwargs)` | partial | `vplAxesContourF` | explicit levels only, extend='neither'; no automatic levels, no hatching |
| `csd(x, y, *, NFFT=None, Fs=None, Fc=None, detrend=None, window=None, noverlap=None, pad_...)` | yes | `vplAxesCsd` | same helper and knobs as psd |
| `ecdf(x, weights=None, *, complementary=False, orientation='vertical', compress=False, dat...)` | yes | `vplAxesEcdf` | ascending or complementary, weighted, either orientation; compress is a dedup that only speeds drawing |
| `errorbar(x, y, yerr=None, xerr=None, fmt='', *, ecolor=None, elinewidth=None, capsize=None, b...)` | partial | `vplAxesErrorBar` | symmetric xerr and yerr; no asymmetric pairs, no errorevery, no fmt |
| `eventplot(positions, *, orientation='horizontal', lineoffsets=1, linelengths=1, linewidths=Non...)` | partial | `vplAxesEventPlot` | one row per call, both orientations; no per-row offsets or lengths |
| `fill(*args, data=None, **kwargs)` | yes | `vplAxesFill` | one polygon per call rather than matplotlib's x,y,[fmt] triples in one call -- N calls draw the identical N polygons, each still taking its own colour from the same cycle a single multi-triple call would, so nothing is actually unreachable |
| `fill_between(x, y1, y2=0, where=None, interpolate=False, step=None, *, data=None, **kwargs)` | yes | `vplAxesFillBetween` | the `where` mask, `interpolate`, and all three `step` modes |
| `fill_betweenx(y, x1, x2=0, where=None, step=None, interpolate=False, *, data=None, **kwargs)` | yes | `vplAxesFillBetweenX` | the `where` mask, `interpolate`, and all three `step` modes |
| `get_legend_handles_labels(legend_handler_map=None)` | partial | `vplAxesGetLegendLabel` | the labels, in matplotlib's order; the handles are omitted because a C ABI caller already holds each artist's own handle |
| `get_title(loc='center')` | yes | `vplAxesGetTitle` | the centre title, which is the only one stored |
| `grouped_bar(heights, *, positions=None, group_spacing=1.5, bar_spacing=0, tick_labels=None, labe...)` | partial | `vplAxesGroupedBar` | vertical bars at integer positions with tick and dataset labels; no horizontal orientation, no explicit positions, no colour list |
| `hexbin(x, y, C=None, *, gridsize=100, bins=None, xscale='linear', yscale='linear', extent=N...)` | partial | `vplAxesHexbin` | counts in viridis at a chosen gridsize; no C/reduce_C_function, mincnt, extent, log scales, bins='log' or a chosen colormap |
| `hist(x, bins=None, *, range=None, density=False, weights=None, cumulative=False, bottom=N...)` | partial | `vplAxesHist` | counts or density over equal bins; no weights, stacking, explicit bin edges or step/stepfilled types |
| `hist2d(x, y, bins=10, *, range=None, density=False, weights=None, cmin=None, cmax=None, dat...)` | partial | `vplAxesHist2D` | equal bins over the data range; no explicit edges, weights or density |
| `hlines(y, xmin, xmax, colors=None, linestyles='solid', *, label='', data=None, **kwargs)` | yes | `vplAxesHLines` | colour, width and style |
| `imshow(X, cmap=None, norm=None, *, aspect=None, interpolation=None, alpha=None, vmin=None, ...)` | partial | `vplAxesImshow` | one filled quad per cell, so no interpolation and no alpha channel; extent and origin are fixed to the defaults |
| `indicate_inset(bounds=None, inset_ax=None, *, transform=None, facecolor='none', edgecolor='0.5', al...)` | partial | `vplAxesIndicateInset` | the rectangle and the two visible connectors; no styling arguments, no access to the other two |
| `indicate_inset_zoom(inset_ax, **kwargs)` | partial | `vplAxesIndicateInsetZoom` | the inset limits as the rectangle |
| `inset_axes(bounds, *, transform=None, zorder=5, **kwargs)` | partial | `vplFigureInsetAxes` | bounds in the parent axes fractions, resolved once; no locator that follows the parent, no transform= |
| `legend(*args, **kwargs)` | partial | `vplAxesSetLegend` | all ten locations, a title (vplAxesSetLegendTitle) and ncols (vplAxesSetLegendNCols), but no explicit handle list, no bbox_to_anchor |
| `loglog(*args, **kwargs)` | yes | `vplAxesSetXScale` | as set_xscale + set_yscale, which is what it is |
| `magnitude_spectrum(x, *, Fs=None, Fc=None, window=None, pad_to=None, sides=None, scale=None, data=None,...)` | yes | `vplAxesMagnitudeSpectrum` | linear or dB scale, plus the window, detrend, sides and pad_to knobs |
| `matshow(Z, **kwargs)` | yes | `vplAxesMatShow` | including the top ticks and integer locators |
| `pcolor(*args, shading=None, alpha=None, norm=None, cmap=None, vmin=None, vmax=None, coloriz...)` | partial | `vplAxesPcolorMesh` | pcolormesh stands in; the two differ mainly in how they handle masked cells |
| `pcolorfast(*args, alpha=None, norm=None, cmap=None, vmin=None, vmax=None, colorizer=None, data=...)` | yes | `vplAxesPcolorMesh` | pcolormesh stands in. pcolorfast exists to pick a faster artist for regular grids; vplot emits the same quads either way, so the renderable output is identical and only a speed characteristic, not a capability, differs |
| `pcolormesh(*args, alpha=None, norm=None, cmap=None, vmin=None, vmax=None, colorizer=None, shadi...)` | partial | `vplAxesPcolorMesh` | rectangular grids given by their edges; no curvilinear meshes, no shading modes |
| `phase_spectrum(x, *, Fs=None, Fc=None, window=None, pad_to=None, sides=None, data=None, **kwargs)` | yes | `vplAxesPhaseSpectrum` | with the window, detrend, sides and pad_to knobs |
| `pie(x, *, explode=None, labels=None, colors=None, autopct=None, pctdistance=0.6, shadow=...)` | partial | `vplAxesPie` | wedges and the axes setup; no labels, autopct, explode, startangle or shadow |
| `pie_label(container, /, labels, *, distance=0.6, textprops=None, rotate=False, alignment='auto')` | partial | `vplAxesPieLabel` | centred labels at a chosen distance; no format strings, no rotate=, no outer alignment |
| `plot(*args, scalex=True, scaley=True, data=None, **kwargs)` | yes | `vplAxesPlot` | colour, width, style, markers, label; vplAxesPlotY is the one-argument form, since C cannot overload |
| `psd(x, *, NFFT=None, Fs=None, Fc=None, detrend=None, window=None, noverlap=None, pad_to=...)` | yes | `vplAxesPsd` | Welch's method with the window, detrend, sides and pad_to knobs |
| `quiver(*args, data=None, **kwargs)` | partial | `vplAxesQuiver` | units='width' and angles='uv' with matplotlib's auto-scaling; no scale_units, no per-arrow colour or colormap, no masked arrays |
| `quiverkey(Q, X, Y, U, label, **kwargs)` | partial | `vplAxesQuiverKey` | labelpos='N' in axes coordinates; no E/S/W positions, no angle=, no separate label colour |
| `scatter(x, y, s=None, c=None, *, marker=None, cmap=None, norm=None, vmin=None, vmax=None, al...)` | yes | `vplAxesScatter` | per-point size and colour, colormaps |
| `secondary_xaxis(location, functions=None, *, transform=None, **kwargs)` | partial | `vplFigureSecondaryXAxis` | affine functions only, so ticks stay evenly spaced; no arbitrary function pair, no FuncScale |
| `secondary_yaxis(location, functions=None, *, transform=None, **kwargs)` | partial | `vplFigureSecondaryYAxis` | affine functions only, as for secondary_xaxis |
| `semilogx(*args, **kwargs)` | yes | `vplAxesSetXScale` |  |
| `semilogy(*args, **kwargs)` | yes | `vplAxesSetYScale` |  |
| `set(*, adjustable=<UNSET>, agg_filter=<UNSET>, alpha=<UNSET>, anchor=<UNSET>, animated=<...)` | n/a |  | a Python keyword-argument dispatcher over every other setter -- it has no meaning in a C ABI, where each property has its own entry point |
| `set_title(label, fontdict=None, loc=None, pad=None, *, y=None, **kwargs)` | yes | `vplAxesSetTitle` |  |
| `specgram(x, *, NFFT=None, Fs=None, Fc=None, detrend=None, window=None, noverlap=None, cmap=No...)` | yes | `vplAxesSpecgram` | all four modes, both scales, xextent, a colormap, and the window/detrend/sides/pad_to knobs |
| `spy(Z, *, precision=0, marker=None, markersize=None, aspect='equal', origin='upper', **k...)` | partial | `vplAxesSpy` | the image form only; no marker form for very sparse matrices, no precision= |
| `stackplot(axes, x, *args, labels=(), colors=None, baseline='zero', data=None, **kwargs)` | yes | `vplAxesStackPlot` | all four baselines: zero, sym, wiggle and the weighted_wiggle streamgraph |
| `stairs(values, edges=None, *, orientation='vertical', baseline=0, fill=False, data=None, **...)` | yes | `vplAxesStairs` | fill, a scalar or None baseline, and either orientation; a per-step baseline array is a rare form not offered |
| `stem(*args, linefmt=None, markerfmt=None, basefmt=None, bottom=0, label=None, orientation...)` | yes | `vplAxesStem` | linefmt, markerfmt, basefmt and bottom; horizontal orientation is a rare form not offered |
| `step(x, y, *args, where='pre', data=None, **kwargs)` | yes | `vplAxesStep` | all three drawstyles: pre, post and mid |
| `streamplot(axes, x, y, u, v, density=1, linewidth=None, color=None, cmap=None, norm=None, arrow...)` | partial | `vplAxesStreamPlot` | the RK12 integrator, mask and spiral seeding at a chosen density; no per-line colour or width, no start_points, no integration_direction |
| `table(ax, cellText=None, cellColours=None, cellLoc='right', colWidths=None, rowLabels=None...)` | partial | `vplAxesTable` | cell text, both label headers, column widths, all eighteen locs; no per-cell colours, no edges= modes, no explicit bbox, no auto font size |
| `text(x, y, s, fontdict=None, **kwargs)` | yes | `vplAxesText` | alignment, rotation, both rotation modes |
| `tricontour(ax, *args, **kwargs)` | partial | `vplAxesTriContour` | explicit levels over a computed Delaunay grid; no explicit triangles, mask or level count; clabel is structured-grid only |
| `tricontourf(ax, *args, **kwargs)` | partial | `vplAxesTriContourF` | explicit levels over a computed Delaunay grid; no explicit triangles, mask, level count or extend= |
| `tripcolor(ax, *args, alpha=1.0, norm=None, cmap=None, vmin=None, vmax=None, shading='flat', fa...)` | partial | `vplAxesTriPcolor` | flat shading over a computed Delaunay grid; no gouraud, explicit triangles, mask or facecolors |
| `triplot(ax, *args, **kwargs)` | partial | `vplAxesTriplot` | the computed Delaunay grid as lines; no explicit triangles, no mask, no marker form |
| `violin(vpstats, positions=None, *, vert=None, orientation='vertical', widths=0.5, showmeans...)` | partial | `vplAxesViolinPlot` | the precomputed-statistics form is not separated out; violinplot does the estimate itself |
| `violinplot(dataset, positions=None, *, vert=None, orientation='vertical', widths=0.5, showmeans...)` | partial | `vplAxesViolinPlot` | Scott's bandwidth and the default styling; no showmedians, no custom positions, widths or bw_method |
| `vlines(x, ymin, ymax, colors=None, linestyles='solid', *, label='', data=None, **kwargs)` | yes | `vplAxesVLines` | colour, width and style |
| `xcorr(x, y, *, normed=True, detrend=<function detrend_none at 0x00000187D4B5C510>, usevlin...)` | partial | `vplAxesXCorr` | usevlines with normed; no detrend, no line form |

**75 of 76** -- 35 full, 40 partial, 0 missing, 1 out of scope.

### Axes: limits, scales, ticks and the rest

Inherited from `_AxesBase`. Much of it is paired getters and setters for state vplot keeps as plain struct fields, and some of it (navigation callbacks, transforms, units) belongs to a GUI or to Python and has no C equivalent.

| matplotlib | in vplot | vplot symbol | notes |
| --- | --- | --- | --- |
| `ArtistList(axes, prop_name, valid_types=None, invalid_types=None)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_artist(a)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_child_axes(ax)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_collection(collection, autolim=True)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_container(container)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_image(image)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_line(line)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_patch(p)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_table(tab)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `apply_aspect(position=None)` | n/a |  | internal: vplot applies the aspect in display_box during layout, with no separate step for a caller to call |
| `artists` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `autoscale(enable=True, axis='both', tight=None)` | yes | `vplAxesAutoscale` | enable, per-axis, and tight |
| `autoscale_view(tight=None, scalex=True, scaley=True)` | partial | `vplAxesAutoscale` | no tight= or per-axis arguments; the same entry autoscale maps to |
| `axis(arg=None, /, *, emit=True, **kwargs)` | partial | `vplAxesSetViewLimits` | the [x0, x1, y0, y1] form only; none of the 'equal'/'off'/'tight' words |
| `can_pan()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `can_zoom()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `cla()` | n/a |  | figures are created and destroyed, not reused |
| `clear()` | n/a |  | figures are created and destroyed, not reused |
| `collections` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `contains(mouseevent)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `contains_point(point)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `drag_pan(button, key, x, y)` | yes | `vplAxesPan` | the pan half of interactive navigation |
| `draw(renderer)` | yes | `vplFigureRenderRGBA` | renders into a caller-owned buffer |
| `draw_artist(a)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `end_pan()` | yes | `vplAxesPan` |  |
| `format_coord(x, y)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `format_xdata(x)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `format_ydata(y)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `get_adjustable()` | yes | `vplAxesGetAdjustable` |  |
| `get_anchor()` | partial | `vplAxesGetAnchor` | returns the pair, not the name |
| `get_aspect()` | yes | `vplAxesGetAspect` |  |
| `get_autoscale_on()` | yes | `vplAxesGetAutoscale` |  |
| `get_autoscalex_on()` | yes | `vplAxesGetAutoscaleX` |  |
| `get_autoscaley_on()` | yes | `vplAxesGetAutoscaleY` |  |
| `get_axes_locator()` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `get_axisbelow()` | yes | `vplAxesGetAxisBelow` |  |
| `get_box_aspect()` | yes | `vplAxesGetBoxAspect` |  |
| `get_children()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_data_ratio()` | yes | `vplAxesGetDataRatio` |  |
| `get_default_bbox_extra_artists()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_facecolor()` | yes | `vplAxesGetFaceColor` | and vplFigureGetFaceColor for the figure's own patch -- both classes carry this name, and STATUS is keyed by the name alone |
| `get_fc()` | yes | `vplAxesGetFaceColor` |  |
| `get_forward_navigation_events()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `get_frame_on()` | yes | `vplAxesGetFrameOn` |  |
| `get_gridspec()` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `get_images()` | yes | `vplAxesGetImage` | the deprecated-alias form of the images property above |
| `get_legend()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_lines()` | yes | `vplAxesGetLine` | the deprecated-alias form of the lines property above |
| `get_navigate()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `get_navigate_mode()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `get_position(original=False)` | yes | `vplAxesGetPosition` | left/bottom/width/height |
| `get_rasterization_zorder()` | n/a |  | the mixed vector/raster rasterization boundary, which only a backend that does both at once needs |
| `get_shared_x_axes()` | n/a |  | matplotlib's symmetric N-way sharing Groupers; vplot's sharing is a one-directional follow with no group object to return |
| `get_shared_y_axes()` | n/a |  | matplotlib's symmetric N-way sharing Groupers; vplot's sharing is a one-directional follow with no group object to return |
| `get_subplotspec()` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `get_tightbbox(renderer=None, *, call_axes_locator=True, bbox_extra_artists=None, for_layout_only=F...)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_window_extent(renderer=None)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_xaxis()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_xaxis_text1_transform(pad_points)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_xaxis_text2_transform(pad_points)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_xaxis_transform(which='grid')` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_xbound()` | yes | `vplAxesGetViewLimits` |  |
| `get_xgridlines()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_xinverted()` | yes | `x_inverted` | C++ field only, no ABI getter |
| `get_xlabel()` | yes | `vplAxesGetXLabel` |  |
| `get_xlim()` | yes | `vplAxesGetViewLimits` | both axes at once |
| `get_xmajorticklabels()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_xmargin()` | yes | `vplAxesGetXMargin` |  |
| `get_xminorticklabels()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_xscale()` | yes | `vplAxesGetXScale` |  |
| `get_xticklabels(minor=False, which=None)` | partial | `vplAxesGetXTickLabels` | the strings, not Text objects; major ticks only |
| `get_xticklines(minor=False)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_xticks(*, minor=False)` | yes | `vplAxesGetXTicks` | major ticks only, at the last-rendered pixel size |
| `get_yaxis()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_yaxis_text1_transform(pad_points)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_yaxis_text2_transform(pad_points)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_yaxis_transform(which='grid')` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_ybound()` | yes | `vplAxesGetViewLimits` |  |
| `get_ygridlines()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_yinverted()` | yes | `y_inverted` | C++ field only, no ABI getter |
| `get_ylabel()` | yes | `vplAxesGetYLabel` |  |
| `get_ylim()` | yes | `vplAxesGetViewLimits` | both axes at once |
| `get_ymajorticklabels()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_ymargin()` | yes | `vplAxesGetYMargin` |  |
| `get_yminorticklabels()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_yscale()` | yes | `vplAxesGetYScale` |  |
| `get_yticklabels(minor=False, which=None)` | partial | `vplAxesGetYTickLabels` | the strings, not Text objects; major ticks only |
| `get_yticklines(minor=False)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_yticks(*, minor=False)` | yes | `vplAxesGetYTicks` | major ticks only, at the last-rendered pixel size |
| `grid(visible=None, which='major', axis='both', **kwargs)` | yes | `vplAxesSetGrid` | visible, which (major/minor/both) and axis (x/y/both); styling is set through the grid.* fields, as elsewhere |
| `has_data()` | yes | `vplAxesHasData` | true if any artist has been added |
| `images` | yes | `vplAxesGetImage` | with vplAxesGetImageCount; the handles imshow/matshow/figimage already returned, retrievable again by index |
| `in_axes(mouseevent)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `invert_xaxis()` | yes | `vplAxesSetXInverted` |  |
| `invert_yaxis()` | yes | `vplAxesSetYInverted` |  |
| `label_outer(remove_inner_ticks=False)` | yes | `vplAxesLabelOuter` | labels, and the inner tick marks too with remove_inner_ticks |
| `lines` | yes | `vplAxesGetLine` | with vplAxesGetLineCount; the handles plot/step/stem/... already returned, retrievable again by index |
| `locator_params(axis='both', tight=None, **kwargs)` | partial | `vplAxesLocatorParamsFull` | nbins, steps and tight, via the Full variant (vplAxesLocatorParams is the nbins-only convenience); no per-locator keywords other than MaxNLocator's own (integer, symmetric, prune, min_n_ticks) |
| `margins(*margins, x=None, y=None, tight=True)` | yes | `vplAxesSetMargins` | both axes at once, which is the common call |
| `minorticks_off()` | yes | `vplAxesSetMinorTicks` |  |
| `minorticks_on()` | yes | `vplAxesSetMinorTicks` |  |
| `patches` | yes | `vplAxesGetPatch` | with vplAxesGetPatchCount; the handles bar/fill/... already returned, retrievable again by index |
| `redraw_in_frame()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `relim(visible_only=False)` | n/a |  | data limits are recomputed from the artists on every draw, so there is no cached extent to invalidate |
| `reset_position()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `set_adjustable(adjustable, share=False)` | partial | `vplAxesSetAdjustable` | 'box' only, which is the default; 'datalim' is refused rather than silently treated as 'box' |
| `set_anchor(anchor, share=False)` | partial | `vplAxesSetAnchor` | as a pair of fractions rather than the 'SW'..'NE' names; no 'C' string form |
| `set_aspect(aspect, adjustable=None, anchor=None, share=False)` | partial | `vplAxesSetAspect` | a number or 0 for auto; no 'datalim' adjustable mode |
| `set_autoscale_on(b)` | yes | `vplAxesSetAutoscale` |  |
| `set_autoscalex_on(b)` | yes | `vplAxesSetAutoscaleX` |  |
| `set_autoscaley_on(b)` | yes | `vplAxesSetAutoscaleY` |  |
| `set_axes_locator(locator)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `set_axis_off()` | yes | `vplAxesSetAxisOn` | pass 0; hides the frame, ticks, grid and axis labels, keeps data and title |
| `set_axis_on()` | yes | `vplAxesSetAxisOn` | pass 1 |
| `set_axisbelow(b)` | yes | `vplAxesSetAxisBelow` | the three states On/Line/Off, matching True/'line'/False |
| `set_box_aspect(aspect=None)` | yes | `vplAxesSetBoxAspect` |  |
| `set_facecolor(color)` | yes | `vplAxesSetFaceColor` | and vplFigureSetFaceColor for the figure's own patch |
| `set_fc(color)` | yes | `vplAxesSetFaceColor` |  |
| `set_figure(fig)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `set_forward_navigation_events(forward)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `set_frame_on(b)` | yes | `vplAxesSetFrameOn` | all four spines and the background together |
| `set_navigate(b)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `set_navigate_mode(b)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `set_position(pos, which='both')` | yes | `vplAxesSetPosition` | left/bottom/width/height, and it leaves the grid like add_axes |
| `set_prop_cycle(*args, **kwargs)` | yes | `vplAxesSetPropCycleFull` | colour, linestyle, marker and linewidth zipped together (vplAxesSetPropCycle is the colour-only convenience form); linestyle/marker/linewidth only feed plot(), the same as matplotlib's own cycler only ever gives those three to Line2D |
| `set_rasterization_zorder(z)` | n/a |  | the mixed vector/raster rasterization boundary, which only a backend that does both at once needs |
| `set_subplotspec(subplotspec)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `set_xbound(lower=None, upper=None)` | yes | `vplAxesSetXBound` | NAN for either argument means 'leave this end alone', matplotlib's None |
| `set_xinverted(inverted)` | yes | `vplAxesSetXInverted` |  |
| `set_xlabel(xlabel, fontdict=None, labelpad=None, *, loc=None, **kwargs)` | yes | `vplAxesSetXLabel` |  |
| `set_xlim(left=None, right=None, *, emit=True, auto=False, xmin=None, xmax=None)` | yes | `vplAxesSetXLim` |  |
| `set_xmargin(m)` | yes | `vplAxesSetXMargin` | the single-axis setter, down to matplotlib's -0.5 floor |
| `set_xscale(value, **kwargs)` | partial | `vplAxesSetXScale` | linear and log; no symlog, logit or function scales |
| `set_xticklabels(labels, *, minor=False, fontdict=None, **kwargs)` | partial | `vplAxesGetXTicks` | read the current positions here, then relabel them with vplAxesSetXTicks; no minor=True, no fontdict |
| `set_xticks(ticks, labels=None, *, minor=False, **kwargs)` | yes | `vplAxesSetXTicks` | positions and labels, and the empty list |
| `set_ybound(lower=None, upper=None)` | yes | `vplAxesSetYBound` | NAN for either argument means 'leave this end alone', matplotlib's None |
| `set_yinverted(inverted)` | yes | `vplAxesSetYInverted` |  |
| `set_ylabel(ylabel, fontdict=None, labelpad=None, *, loc=None, **kwargs)` | yes | `vplAxesSetYLabel` |  |
| `set_ylim(bottom=None, top=None, *, emit=True, auto=False, ymin=None, ymax=None)` | yes | `vplAxesSetYLim` |  |
| `set_ymargin(m)` | yes | `vplAxesSetYMargin` |  |
| `set_yscale(value, **kwargs)` | partial | `vplAxesSetYScale` | linear and log; no symlog, logit or function scales |
| `set_yticklabels(labels, *, minor=False, fontdict=None, **kwargs)` | partial | `vplAxesGetYTicks` | read the current positions here, then relabel them with vplAxesSetYTicks; no minor=True, no fontdict |
| `set_yticks(ticks, labels=None, *, minor=False, **kwargs)` | yes | `vplAxesSetYTicks` | positions and labels, and the empty list |
| `set_zorder(level)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `sharex(other)` | partial | `vplAxesShareX` | one-directional follow, not matplotlib's symmetric N-way group -- set_xlim on the follower does not reach back to move the axes it follows |
| `sharey(other)` | partial | `vplAxesShareY` | one-directional follow, not matplotlib's symmetric N-way group -- set_ylim on the follower does not reach back to move the axes it follows |
| `start_pan(x, y, button)` | yes | `vplAxesPan` |  |
| `tables` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `texts` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `tick_params(axis='both', **kwargs)` | partial | `vplAxesSetTickParams` | direction, length, width, pad, label size, visibility, and colors=/labelcolor= as RGBA (no colour-spec string); no per-side control or minor/major split |
| `ticklabel_format(*, axis='both', style=None, scilimits=None, useOffset=None, useLocale=None, useMathT...)` | partial | `vplAxesTickLabelFormat` | the offset and scientific-notation switches and the power limits; no useLocale, no useMathText |
| `twinx(axes_class=None, *, delta_zorder=0.0, **kwargs)` | yes | `vplFigureTwinX` |  |
| `twiny(axes_class=None, *, delta_zorder=0.0, **kwargs)` | yes | `vplFigureTwinY` |  |
| `update_datalim(xys, updatex=True, updatey=True)` | partial | `vplAxesUpdateDataLim` | one rectangle at a time; no updatex/updatey switches |
| `use_sticky_edges` | yes | `vplAxesSetUseStickyEdges` | a caller can turn the sticky clamp off, and read it back |
| `viewLim` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `xaxis_date(tz=None)` | n/a |  | date and unit machinery vplot has none of: axes carry plain doubles, so there is no unit registry to switch a converter in |
| `xaxis_inverted()` | yes | `x_inverted` | C++ field only, no ABI getter |
| `yaxis_date(tz=None)` | n/a |  | date and unit machinery vplot has none of: axes carry plain doubles, so there is no unit registry to switch a converter in |
| `yaxis_inverted()` | yes | `y_inverted` | C++ field only, no ABI getter |

**91 of 160** -- 73 full, 18 partial, 0 missing, 69 out of scope.

### Figure

Everything `Figure` and its bases define, minus the `Artist` methods listed at the end.

| matplotlib | in vplot | vplot symbol | notes |
| --- | --- | --- | --- |
| `add_artist(artist, clip=False)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `add_axes(*args, **kwargs)` | partial | `vplFigureAddAxes` | an explicit rect only, no projections or sharing |
| `add_axobserver(func)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `add_gridspec(nrows=1, ncols=1, **kwargs)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `add_subfigure(subplotspec, **kwargs)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `add_subplot(*args, **kwargs)` | partial | `vplFigureAddSubplot` | a regular grid only, no gridspec or spans |
| `align_labels(axs=None)` | yes | `vplFigureAlignLabels` | both axis labels at once, like matplotlib's |
| `align_titles(axs=None)` | yes | `vplFigureAlignTitles` | aligns titles to the group's topmost frame; vplot places no variable decoration above the frame for a title to clear, so that frame top is the whole of what there is to align to |
| `align_xlabels(axs=None)` | yes | `vplFigureAlignXLabels` | groups by shared row, aligns to the group's furthest tick edge |
| `align_ylabels(axs=None)` | yes | `vplFigureAlignYLabels` | groups by shared column, aligns to the group's furthest tick edge |
| `autofmt_xdate(bottom=0.2, rotation=30, ha='right', which='major')` | n/a |  | date and unit machinery vplot has none of: axes carry plain doubles, so there is no unit registry to switch a converter in |
| `axes` | yes | `vplFigureGetAxes` | the property form of get_axes |
| `clear(keep_observers=False)` | n/a |  | figures are created and destroyed, not reused |
| `clf(keep_observers=False)` | yes | `vplFigureClear` | drops the axes and sup-labels; keep_observers governs matplotlib's figure-change callbacks, which a headless renderer has none of |
| `colorbar(mappable, cax=None, ax=None, use_gridspec=True, **kwargs)` | partial | `vplFigureColorbarOriented` | vertical or horizontal (vplFigureColorbar is the plain vertical form); attached to one axes, default ticks |
| `contains(mouseevent)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `delaxes(ax)` | yes | `vplFigureDelAxes` |  |
| `dpi` | n/a |  | an attribute rather than a method: read or set through the matching ABI call where one exists |
| `draw(renderer)` | yes | `vplFigureRenderRGBA` | renders into a caller-owned buffer |
| `draw_artist(a)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `draw_without_rendering()` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `figimage(X, xo=0, yo=0, alpha=None, norm=None, cmap=None, vmin=None, vmax=None, origin=None, ...)` | partial | `vplFigureFigImage` | a scalar array through a colormap, at native resolution and a pixel offset, origin='upper'; the direct-RGBA array and origin='lower' are not exposed yet |
| `figure` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `frameon` | n/a |  | an attribute rather than a method: read or set through the matching ABI call where one exists |
| `gca()` | yes | `vplFigureGca` | adds an axes when the figure has none, as matplotlib's does |
| `get_axes()` | yes | `vplFigureGetAxes` | count then index, since a C ABI enumerates rather than returning a list |
| `get_children()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_constrained_layout()` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `get_constrained_layout_pads(relative=False)` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `get_default_bbox_extra_artists()` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_dpi()` | yes | `vplFigureGetDpi` |  |
| `get_edgecolor()` | yes | `vplFigureGetEdgeColor` |  |
| `get_facecolor()` | yes | `vplAxesGetFaceColor` | and vplFigureGetFaceColor for the figure's own patch -- both classes carry this name, and STATUS is keyed by the name alone |
| `get_figheight()` | yes | `vplFigureGetSizeInches` | both dimensions at once |
| `get_figure(root=None)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `get_figwidth()` | yes | `vplFigureGetSizeInches` | both dimensions at once |
| `get_frameon()` | yes | `vplFigureGetFrameOn` |  |
| `get_layout_engine()` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `get_linewidth()` | yes | `vplFigureGetLineWidth` |  |
| `get_size_inches()` | yes | `vplFigureGetSizeInches` | both dimensions, in inches |
| `get_suptitle()` | yes | `vplFigureGetSupTitle` |  |
| `get_supxlabel()` | yes | `vplFigureGetSupXLabel` |  |
| `get_supylabel()` | yes | `vplFigureGetSupYLabel` |  |
| `get_tight_layout()` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `get_tightbbox(renderer=None, *, bbox_extra_artists=None)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `get_window_extent(renderer=None)` | n/a |  | transform objects, which vplot does not expose: it maps data to pixels internally and hands back neither the transform nor a display-space bbox |
| `ginput(n=1, timeout=30, show_clicks=True, mouse_add=<MouseButton.LEFT: 1>, mouse_pop=<Mouse...)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `legend(*args, **kwargs)` | partial | `vplAxesSetLegend` | all ten locations, a title (vplAxesSetLegendTitle) and ncols (vplAxesSetLegendNCols), but no explicit handle list, no bbox_to_anchor |
| `number` | n/a |  | an attribute rather than a method: read or set through the matching ABI call where one exists |
| `pick(mouseevent)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `savefig(fname, *, transparent=None, **kwargs)` | partial | `vplFigureSaveFig` | png, svg and pdf; no eps, ps or dpi override |
| `sca(a)` | yes | `vplFigureSca` | with vplFigureGca: matplotlib's own sca/gca pair is pyplot global state consulted by every other implicit-axes pyplot.* call, which this ABI has none of -- every entry point already takes the axes it acts on. current_axes is consulted by nothing but vplFigureGca itself, which is the whole of what sca/gca can mean here |
| `set(*, agg_filter=<UNSET>, alpha=<UNSET>, animated=<UNSET>, canvas=<UNSET>, clip_box=<UN...)` | n/a |  | a Python keyword-argument dispatcher over every other setter -- it has no meaning in a C ABI, where each property has its own entry point |
| `set_canvas(canvas)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `set_constrained_layout(constrained)` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `set_constrained_layout_pads(**kwargs)` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `set_dpi(val)` | yes | `vplFigureSetDpi` |  |
| `set_edgecolor(color)` | yes | `vplFigureSetEdgeColor` |  |
| `set_facecolor(color)` | yes | `vplAxesSetFaceColor` | and vplFigureSetFaceColor for the figure's own patch |
| `set_figheight(val, forward=True)` | yes | `vplFigureSetFigHeight` | the single-dimension setter |
| `set_figure(fig)` | n/a |  | the scene graph vplot does not have: it takes DATA and draws it, so there are no free-standing artist objects to add, list, reorder or hand back |
| `set_figwidth(val, forward=True)` | yes | `vplFigureSetFigWidth` | the single-dimension setter |
| `set_frameon(b)` | yes | `vplFigureSetFrameOn` |  |
| `set_layout_engine(layout=None, **kwargs)` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `set_linewidth(linewidth)` | yes | `vplFigureSetLineWidth` |  |
| `set_size_inches(w, h=None, forward=True)` | yes | `vplFigureSetSizeInches` |  |
| `set_tight_layout(tight)` | n/a |  | pluggable layout engines: vplot has tight_layout as a one-shot call, not an engine object with pads and a constrained variant |
| `show(warn=True)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |
| `subfigures(nrows=1, ncols=1, squeeze=True, wspace=None, hspace=None, width_ratios=None, height_...)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `subplot_mosaic(mosaic, *, sharex=False, sharey=False, width_ratios=None, height_ratios=None, empty_...)` | n/a |  | locator, gridspec and subfigure objects vplot has no equivalent of: its grid is the subplot formula, not a manipulable object |
| `subplots(nrows=1, ncols=1, *, sharex=False, sharey=False, squeeze=True, width_ratios=None, he...)` | partial | `vplFigureSubplotsShared` | sharex/sharey ('none'/'all'/'row'/'col', one-directional -- see vplAxesShareX's note); vplFigureSubplots is the plain grid with no sharing. Neither has gridspec_kw, width_ratios/height_ratios or squeeze |
| `subplots_adjust(left=None, bottom=None, right=None, top=None, wspace=None, hspace=None)` | yes | `vplFigureSubplotsAdjust` | all six parameters, and it re-lays the subplots already added |
| `suptitle(t, **kwargs)` | yes | `vplFigureSuptitle` | text, x, y and fontsize; general Text kwargs (weight, alignment) are not a suptitle-specific gap |
| `supxlabel(t, **kwargs)` | yes | `vplFigureSupXLabel` | text, x, y and fontsize |
| `supylabel(t, **kwargs)` | yes | `vplFigureSupYLabel` | text, x, y and fontsize |
| `text(x, y, s, fontdict=None, **kwargs)` | yes | `vplAxesText` | alignment, rotation, both rotation modes |
| `tight_layout(*, pad=1.08, h_pad=None, w_pad=None, rect=None)` | yes | `vplFigureTightLayout` |  |
| `waitforbuttonpress(timeout=-1)` | n/a |  | interactive navigation and hit-testing, which need a GUI event loop the renderer does not have -- it draws a buffer and returns |

**43 of 78** -- 36 full, 7 partial, 0 missing, 35 out of scope.

### Instance attributes

matplotlib attaches these to the object in `__init__`, not to the class, so class introspection cannot enumerate them and the tables above do not mention them. This one is written by hand -- treat it as less complete than the generated tables, because it is.

| matplotlib | in vplot | vplot symbol | notes |
| --- | --- | --- | --- |
| `ax.spines[...].set_visible` | yes | `vplAxesSetSpineVisible` | all four edges |
| `ax.spines[...].set_color` | no |  | spines take the axes edgecolor, one colour for all four |
| `ax.spines[...].set_position` | no |  | no zero-crossing or outward spines |
| `ax.spines[...].set_linewidth` | partial | `linewidth` | the Axes field, one width for all four |
| `ax.patch` | partial | `facecolor` | facecolor and visibility as C++ fields, no ABI entry |
| `ax.title` | partial | `vplAxesSetTitle` | text and pad; the Text object itself is not exposed |
| `ax.xaxis / ax.yaxis` | partial | `vplAxesSetTickParams` | the tick styling is reachable; the Axis object, its locator and its formatter are not |
| `ax.xaxis.set_major_locator` | no |  | the locator is chosen by the scale, not by the caller |
| `ax.xaxis.set_major_formatter` | no |  | no custom formatters |
| `ax.transData / ax.transAxes` | no |  | no transform objects; vplot maps data to pixels internally |

### vplot entries with no matplotlib counterpart

Checked in reverse: an ABI entry that answers to nothing in matplotlib has to be justified here, or the script fails.

| vplot | why |
| --- | --- |
| `vplAxesAddStickyEdgeY` | the y half of sticky_edges, whose x half the sticky_edges row names; matplotlib's sticky_edges.x and .y are two lists |
| `vplAxesGetAxisOn` | the read half of set_axis_off/set_axis_on. matplotlib has no get_axis_on -- the state is a private _axis_on flag -- so there is no method name to put this against |
| `vplAxesGetLegendLabelCount` | the count half of get_legend_handles_labels. Python returns a list; a C ABI enumerates by index, so it needs a count call as well as the one this table can name against the method |
| `vplAxesGetMargins` | the read half of margins() called with no arguments, which returns the pair; the `margins` row names the setter half, and get_xmargin/get_ymargin read each on its own |
| `vplAxesGetTickParams` | the read half of tick_params, which matplotlib has no getter for -- it is what lets a caller change one field without restating the others |
| `vplAxesGetUseStickyEdges` | the read half of the use_sticky_edges property, whose setter half the use_sticky_edges row names |
| `vplAxesSetXErr` | matplotlib takes xerr as a keyword to errorbar; a C entry with one parameter per keyword would be unwieldy, so the horizontal half is set on the line the call returned |
| `vplAxesZoomAt` | matplotlib zooms through toolbar callbacks (_set_view_from_bbox), which are private and GUI-shaped; this is the same operation as a plain function |
| `vplCreateFigure` | Figure() and plt.figure(), as a constructor rather than a method |
| `vplDestroyFigure` | no garbage collector to do it |
| `vplFigureGetAxesCount` | the count half of get_axes, for the same reason: a C ABI enumerates by index rather than returning a list |
| `vplFigureGetPixelSize` | the rendered size in pixels -- figure inches times dpi. matplotlib exposes this only through the canvas (get_width_height), not as a Figure method; get_size_inches is the inches form and has its own row |
| `vplFigureGetSubplotParams` | the read half of subplots_adjust. matplotlib takes keyword arguments and leaves the rest alone; a C struct cannot, so the caller reads, edits one field and writes back. get_subplotpars was removed in matplotlib 3.11 |
| `vplGetLastError` | matplotlib raises; a C ABI returns a code and stashes the text |
| `vplResultToString` | same -- turns that code back into something printable |
| `vplSetFontPath` | matplotlib finds fonts through its font manager and rcParams; vplot is told where the file is |

### Artist

The 60 methods every matplotlib artist inherits: zorder, clipping, picking, transforms, URLs, sketch parameters, unit conversion. vplot has no Artist base class and no scene graph -- artists are structs the figure draws in a fixed order -- so these have no counterpart and are not counted as gaps. They are listed because a coverage document that quietly drops a category is not a coverage document.

```
add_callback  axes  convert_xunits  convert_yunits  figure  findobj
format_cursor_data  get_agg_filter  get_alpha  get_animated  get_clip_box  get_clip_on
get_clip_path  get_cursor_data  get_figure  get_gid  get_in_layout  get_label
get_mouseover  get_path_effects  get_picker  get_rasterized  get_sketch_params  get_snap
get_transform  get_transformed_clip_path_and_affine  get_url  get_visible  get_zorder  have_units
is_transform_set  mouseover  pchanged  pick  pickable  properties
remove  remove_callback  set_agg_filter  set_alpha  set_animated  set_clip_box
set_clip_on  set_clip_path  set_gid  set_in_layout  set_label  set_mouseover
set_path_effects  set_picker  set_rasterized  set_sketch_params  set_snap  set_transform
set_url  set_visible  stale  sticky_edges  update  update_from
```
